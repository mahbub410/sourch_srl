INSERT INTO EPAY_UTILITY_BILL@epay_srl
SELECT * FROM EPAY_UTILITY_BILL--_GPR_X
WHERE  (ACCOUNT_NUMBER,BILL_NUMBER,BILL_MONTH,LOCATION_CODE) IN (
SELECT  ACCOUNT_NUMBER,BILL_NUMBER,BILL_MONTH,LOCATION_CODE FROM EPAY_UTILITY_BILL--_GPR_X
WHERE BILL_MONTH=:P_BILL_MONTH
AND DATA_TRANS_LOG='T'
AND BILL_DUE_DATE>=TRUNC(SYSDATE)
and substr(account_number,1,1)<>'3'
and account_number in (select account_no from epay_customer_master_data@epay_srl)
AND LOCATION_CODE IN (SELECT LOCATION_CODE FROM EPAY_LOCATION_MASTER
where CENTER_NAME='CHITTAGONG' 
)
MINUS
SELECT  ACCOUNT_NUMBER,BILL_NUMBER,BILL_MONTH,LOCATION_CODE FROM EPAY_UTILITY_BILL@epay_srl
WHERE BILL_MONTH=:P_BILL_MONTH
and account_number in (select account_no from epay_customer_master_data@epay_srl)
AND LOCATION_CODE IN (SELECT LOCATION_CODE FROM EPAY_LOCATION_MASTER
where CENTER_NAME='CHITTAGONG'
));

COMMIT;